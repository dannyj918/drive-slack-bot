# Slack Drive Bot — Python dependencies
# Install with: pip install -r requirements.txt

# Slack
slack-bolt>=1.18.0          # Bolt framework (includes Socket Mode support)
websocket-client>=1.6.0     # Required for Socket Mode

# Google Drive
google-api-python-client>=2.100.0
google-auth>=2.23.0
google-auth-httplib2>=0.1.0

# Anthropic
anthropic>=0.40.0

# OpenAI (embeddings only — text-embedding-3-small for RAG)
openai>=1.30.0

# RAG — vector store + PDF parsing
chromadb>=0.5.0
pypdf>=4.0.0
python-pptx>=0.6.23          # .pptx files
python-docx>=1.1.0           # .docx files
openpyxl>=3.1.0              # .xlsx files
beautifulsoup4>=4.12.0       # .html files

# Utilities
python-dotenv>=1.0.0        # Loads .env file automatically
